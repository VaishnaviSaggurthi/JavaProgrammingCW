package basicsOfJava;

public class chardatatype {
    public static void main(String[] args) {
        char ch = 'B';
        // char stores only single characters ex: 'a','v' ; It doesn't store multiple characters ex: 'abc','name'
        // a to z , A to Z , special characters,numbers all are valid
        System.out.println(ch);
    }
}
